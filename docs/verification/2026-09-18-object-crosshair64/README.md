# 小对象阈值与复核文案调整

- 小对象阈值从 32px 提高到 64px，仍按原视频宽、高均不超过阈值判断，且先于 1.25 倍扩框。
- 圆环十字标保持 32×32 CSS 像素，点击切换信息浮层；人脸、坐标转换与原始结果保持现状。
- 结果说明中的重复“（待复核）”删除，时间戳旁的状态标签保留。历史任务刷新页面即可使用新显示规则。

128 项前端测试与生产构建通过。更新已有阈值回归，覆盖 64px 包含、64.001px 排除、
42.24×35.64px 对象改用十字标，以及扩框前分类、长窄对象和人脸保持原框。

使用实际 FaceOverlay 组件进行浏览器验证：64×64 原始像素对象显示圆环十字标，
65×65 对象保留原框，16×16 人脸保持原框；十字标显示尺寸为 32×32 CSS 像素。
点击后浮层实际显示，再点隐藏，浏览器无运行错误。

251 仅更新 WebUI，保留回滚备份并核对前端构建输入，API、Worker 及基础服务容器不重建。
健康状态、前端资源校验和容器记录见 [deployment.json](deployment.json)。
