# 对象框显示放大与定位复查

## 显示调整

- 旗帜、徽标和裸露部位显示框的宽、高各乘 1.25，保持中心，四边限制在图像范围内。
- 人脸框沿用原有逻辑；模型输出、数据库样本坐标和 JSON 导出均不修改。
- 旧任务刷新页面后也使用新显示规则，无需重新提交审核。
- 前端 124 项测试通过，包括对象尺寸与中心、四边截断、人脸尺寸、保存数据不变、重复渲染及 PTS 匹配。

## 用户反馈的定位偏移

任务 `69cc978a-783c-4d30-a3c5-16dff34000ef`，`test_pieces-1.mp4`，
PTS 74.0 秒，第 2220 帧（从 0 开始），原图 1920×1080，推理采样图 1080×607。
复查使用原视频解码帧，没有把用户截图新增的箭头或紫框送入模型。
原视频自身已有一个红色大矩形。

保存的两个模型 bbox（整图 0–1 坐标）为：

| 实例 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 左侧 | 0.333 | 0.822 | 0.022 | 0.033 |
| 镜中 | 0.706 | 0.822 | 0.022 | 0.033 |

原图对应的中心分别为 (660.48, 905.58) 和 (1376.64, 905.58)。
与原图及用户箭头对照，两者中心确实偏离部位，而且横向偏移方向相反。
采样保持宽高比，前端按视频实际内容区域映射坐标，没有发现统一平移或人脸扩框造成的误差。

## 局部定位实验与限制

仍使用 `WasuAI/Qwen3.8-27B-Abliterated`、temperature=0 和线上模型网关。
两组局部区域均从已保存 bbox 中心扩展生成，没有使用箭头位置指导模型。

1. 原生产提示词，左右两张局部图：左侧返回一个仍偏离目标的框，右侧返回空数组。
2. 局部图放大并使用中文精确定位提示：两张图均返回空数组。
3. 英文简短解剖定位提示，整帧及两张放大的局部图：三张图均返回空数组。

局部二次定位未得到可复现的改善，且增加漏检，因此没有接入生产检测分支，也没有改写这条任务的结果。
完整输出见本目录 probe-*.json。1.25 倍仅改善显示留白，不会修正中心；当前样例的偏移仍是模型定位限制。
本轮只发布已验证的显示调整，未重新运行整个视频审核。

## 251 部署

已发布前端镜像 `object-box-125-20260918`，新资源为 `index-DK4tciDv.js`。
API、Worker、InsightFace 及基础服务容器均未重建，8000、8001 健康检查均返回 200；
上述任务的执行次数、更新时间和结果摘要保持不变。详见 [部署验证](deployment.json)。

回滚备份：`/home/aigc/wcm-cluster/backups/object-box-125-20260918`，
含旧源码、环境配置和前端回滚镜像记录。共享 Compose 标签的后端镜像为原运行镜像的同内容别名，
本轮没有修改后端检测逻辑。
