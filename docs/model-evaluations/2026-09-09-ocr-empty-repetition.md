# OCR 空白循环单帧诊断

- 视频：`http://10.252.25.251:18080/videos/test_pieces-1.mp4`，实际采样时间 `8.0` 秒，JPEG 尺寸 `1080 × 607`。画面有地球仪及小字，不是纯色无字画面。
- 使用现有 `WasuAI/PaddleOCR-VL-1.6` 服务、`temperature=0`、`max_tokens=300`，只对这一帧做两次请求。两组均沿用相同图像、流式读取和重复检测逻辑。

| 提示方式 | 耗时 | 输出 | 结束原因 |
| --- | --- | --- | --- |
| 当前 system 字符限制 + `OCR:` | 0.32 秒 | 40 个换行，没有有效文字 | 本地重复检测停止，`repetition_without_text` |
| 移除 system，只发 `OCR:` | 0.44 秒 | 返回 65 个字符，包含画面角落的文字 | 模型正常 `stop` |

这组对照说明附加 system 指令可能触发该帧的空白循环；单个样本不足以保证其他画面均改善，也不证明全部识别文字准确。正常空 OCR 与异常循环无有效输出需保持区分。建议 OCR 使用原生任务提示，继续以 300 tokens 和本地 500 字符截断控制输出；取消提示词中的字符要求需与此前用户要求协调。

原始帧与诊断响应保存在 251 的 `/tmp/wcm/ocr-diagnostic-8s/`。未重跑完整视频审核，也未修改历史审核结果。
