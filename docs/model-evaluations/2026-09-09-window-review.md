# 2026-09-09 窗口审核评估

## 配置

`window` + `scene`，每窗最多三张，跨度上限 10 秒，间隔 1 秒，稳定帧最大步长 3，切镜阈值 27；Qwen 1024 tokens，不追加单图复核。

## 真实视频仅采样统计

251 隔离容器内对 `test_pieces-2.mp4` 解码统计，未将视频帧发送给模型，也未创建或改写历史审核任务。

| 项目 | 数量 |
| --- | ---: |
| 解码帧 | 17,687 |
| 固定网格采样 | 590 |
| 检测切镜 | 81 |
| 短镜头补采 | 2 |
| 保留变化/周期复查帧 | 537 |
| 原场景模式视觉目标请求 | 537 |
| 新窗口计划请求（未计缓存和 fallback） | 208 |
| 三图 / 两图 / 单图窗口 | 142 / 45 / 21 |

同一组选中帧的 Qwen 计划请求减少 329 次（61.3%）。OCR 和人脸各检查最多 537 张，较原 590 次固定网格调用减少约 9%，完全相同输入还可复用。81 次切镜不能等同于 81 次审核请求，因为长镜头内仍可能出现字幕和人物变化。

解码及分窗耗时 26.03 秒；该数据不含模型、下载及整条审核任务耗时。没有测量本视频 OCR/人脸缓存命中率，也没有用本次采样测试证明真实内容召回率。

## 真实 Qwen 合成图测试

使用自行绘制的蓝圆、红三角、绿方块三张图片，调用项目现有 Qwen 服务，使用实际整窗提示词。未发送用户视频或人物图片。

| 输入 | 请求耗时 | 结果 |
| --- | ---: | --- |
| 独立三图：蓝圆、红三角、绿方块 | 4.82 秒 | 正确描述三张及其顺序 |
| 独立三图：绿方块、蓝圆、红三角 | 5.84 秒 | 正确描述三张及其顺序 |
| 三面板拼图：蓝圆、红三角、绿方块 | 5.01 秒 | 保留三张内容，包括第三面板 |

每组只有一次 Qwen 描述请求，没有单图复核。这些测试支持整窗指令遵循和拼图兼容，不能推导裸露、暴力等真实审核内容的准确率。

## 回归验证

新增 16 项窗口测试覆盖最多三图、不跨镜头、有界跨度、后两张内容保留、并发缓存及失败重试、变化字幕/新人物、连续区间、人脸框时间、局部失败、空 OCR 和取消清理。原目标帧回归显式使用 `target`，保留旧模式覆盖。

全量 Python 回归：396 passed、7 skipped；WebUI：31 passed。Ruff 检查与 Git 空白检查通过。
